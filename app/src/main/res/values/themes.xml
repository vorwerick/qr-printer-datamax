<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.QRPrint" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/han_purple</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    <!--    <item name="android:windowSplashScreenBackground">@color/kombu_green</item>
        <item name="android:windowSplashScreenAnimatedIcon">@mipmap/ic_app_round</item>
        <item name="android:windowSplashScreenAnimationDuration">1500</item>-->
    </style>

    <style name="Theme.QRPrint.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.QRPrint.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.QRPrint.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>